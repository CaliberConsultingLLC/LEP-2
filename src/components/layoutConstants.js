export const CONTENT_MAX_WIDTH_DEFAULT = 1180;
export const CONTENT_MAX_WIDTH_WIDE = 1180;

// Shared horizontal padding for both headers and page bodies so their
// content edges stay flush regardless of which page is rendering.
export const CONTENT_PX = { xs: 3, md: 6, lg: 8 };

// Space between the Compass topbar and the main stage when the chapter
// header (porthole + title + description) is hidden.
export const TOPBAR_CONTENT_GAP = { xs: 3, md: 4 };
